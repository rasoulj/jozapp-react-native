/*
import firestoreF from '@react-native-firebase/firestore';
import authF from '@react-native-firebase/auth';

export const auth = authF();
export const firestore = firestoreF();

export const serverTimestamp = firestoreF.FieldValue.serverTimestamp;
export const serverInc1 = firestoreF.FieldValue.increment(2);
export const increment = firestoreF.FieldValue.increment;
export const Timestamp = firestoreF.Timestamp;

*/

// export const serverTimestamp = () => 31;
